apiVersion: velero.io/v1
kind: Restore
metadata:
  name: ${BACKUP_NAME}-secret
  namespace: velero
spec:
  backupName: ${BACKUP_NAME}
  includedNamespaces:
  - '*'
  includedResources:
  - 'secrets'
  labelSelector:
    matchExpressions:
    - {key: radix-app, operator: Exists}