{{- if .Values.createServiceAccountAndRoles -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: "radix-monitoring"
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"

{{- end -}}