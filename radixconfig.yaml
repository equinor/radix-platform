apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: radix-platform
spec:
  environments:
    - name: prod
      build:
        from: master
    - name: RA-37-ExternalAlias
      build:
        from: RA-37-ExternalAlias
  components:
    - name: public-site
      src: "./public-site"
      ports:
       - name: http
         port: 80
      public: true
      environmentConfig:
      - environment: prod
        resources:
          requests:
            memory: "10Mi"
            cpu: "5m"
          limits:
            memory: "20Mi"
            cpu: "10m"
